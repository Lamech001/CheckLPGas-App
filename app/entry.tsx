// expo-router entrypoint (TypeScript)
//
// Some expo-router versions expect route modules to be TS/TSX.
// Keeping a TSX version avoids “missing default export” warnings
// that can occur when using a plain .js entry module.

import 'expo-router/entry';

export default function Entry() {
  return null;
}

